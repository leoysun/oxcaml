{
  "name": "rummikub-ui-tests",
  "version": "1.0.0",
  "description": "UI tests for Rummikub game",
  "scripts": {
    "test": "playwright test",
    "test:ui": "playwright test --ui",
    "test:headed": "playwright test --headed"
  },
  "devDependencies": {
    "@playwright/test": "^1.40.0"
  }
}
